import { createApp } from "vue";
import App from "./App.vue"; // Main Vue component
import "./assets/bootstrap.css"; //Geo-styling

createApp(App).mount("#app"); // Mounts the app to the div with id="app"
